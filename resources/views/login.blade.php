<x-layout>

    <x-slot:btn>
        <a href="{{ route('register') }}" class="btn btn-primary">
            Registre-se
        </a>
    </x-slot:btn>

    Login

    <a href="{{ route('home') }}">Tasks</a>

</x-layout>
